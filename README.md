# nan_client
```sh
$ sudo apt install gnat gprbuild
$ git clone https://github.com/nan-ci/ada_lib.git
$ git clone https://github.com/nan-ci/nan_client.git
$ cd nan_client
$ gprbuild -p nan_client
```
